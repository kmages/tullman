# Exact-match: always wins over other locations
location = /admin {
  add_header X-Admin-Force "1";
  alias /var/www/tullman/_admin_reset.html;
  default_type text/html;
  charset utf-8;
  add_header Cache-Control "no-store";
}
